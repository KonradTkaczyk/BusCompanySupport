<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dateOfTrip, "Date and time of Trip" %><br />
    <%= f.datetime_select :dateOfTrip %>
  </div>

    <div class="field">
    <%= f.label :endOfTrip, "Date and time ending the Trip" %><br />
    <%= f.datetime_select(:endOfTrip,:default => 3.hours.from_now) %>
  </div>

  <div class="field">
    <%= f.label :cityTo, "City to" %> <br />
    <%= f.text_field :cityTo %>
  </div>

  <div class="field">
    <%= f.label :cityFrom, "City from" %> <br />
    <%= f.text_field :cityFrom %>
  </div>

  <div class="field">
    <%= f.label :bus_id, "Name of the bus" %><br />
    <%= collection_select :ticket, :bus_id ,@Buses,:id,:nameOfBus %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

