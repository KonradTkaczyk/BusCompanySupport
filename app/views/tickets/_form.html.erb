<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nameOfTrip %><br />
    <%= f.text_field :nameOfTrip %>
  </div>
  <div class="field">
    <%= f.label :dateOfTrip %><br />
    <%= f.date_select :dateOfTrip %>
  </div>
  <div class="field">
    <%= f.label :timeOfTrip %><br />
    <%= f.time_select :timeOfTrip %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

